#include<bits/stdc++.h>
using namespace std;

int main(){
string str;
getline(cin,str);    //to print the full sentence with spaces 
cout<<str<<endl;     //if we use cin>>str instead of getline then it will print only first word of sentence

  return 0;
}